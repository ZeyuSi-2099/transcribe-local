# 定档变更记录

每一条「定下来的改动」写三件事：**从哪来**（谁定的、依据什么）、**证据在哪**（能复算的产物）、**要不要回流上游**。
研究在内部仓做，这里只进结论；所以这份记录也是「单向同步」的痕迹，谁看都能知道每个数字的出处。

判分尺只有一把：`tools/score.py`。凡引用成绩，注明是哪一次基线、几遍均值。

---

## 2026-09-14

- **定字保持线上同款**（Duner 09-14 定）：`p3.workflow: two_stage` 不动。与线上的三处差异（每批附分歧册、并发 4、撞输出上限对半切）暂时接受，先定其他。
- **按换人拆块**（Duner 09-14 定「路 A」，commit 375b419）：块照旧按长度切给引擎；每个字按 Paraformer 的逐字时间落进声纹分段的哪一段就归谁，四路同步切，只改归属不改文字。
  与声纹分段一致：四路凡 Paraformer 也有的字 100%（每路另有 40–50 个阶段一没给过时间、夹在换人点上的字，按切点位置归）。
  说话人逐字对标准答案：拆前 90.6% → 拆后 98.8%，阶段一上限 98.8%；剩余 1.2% 全是一到十个字的边界错，无半句或整句错。
  线上同款定字（挂 11 条术语库）：18 s 不拆 60.3 / 63.7 / 拿回 35.7 / 回退 2.7 / 说话人 84.2%；
  18 s 拆块 58.0 / 61.5 / 38.0 / 1.0 / 98.2%（n=2，第三遍 DeepSeek 返回 402 余额不足中断）；
  24 s 拆块 52.7 / 58.0 / 43.3 / 3.0 / 97.7%；30 s 拆块 47.7 / 53.7 / 42.3 / 1.7 / 97.9%。
  证据：内部仓 `实测记录/speaker_split_2026-09-14/`。回流：线上的说话人来自云端引擎，不受影响；上游可知会这条做法。
- **默认块长改为 30 s**（Duner 09-14 定）：拆块后说话人不再随块长下降，块长只看内容。`chop.max_length: 30.0`。README 成绩表仍是 18 s 时的实测，已加注；Duner 同日叫停 DeepSeek 实测，新默认的全表暂不重跑。
- **拆块碎片待定**：换人边会切出一两个字的「夹心」碎片（受访者一句话中间一两个字归给主持人）。试验「只把夹心碎片单独问一次模型」：19 个改对 9、改错 1，三遍 17/19 判法一致，逐字说话人 98.76% → 98.84–98.87%。不改定字提示词。**Duner 否决这条路**：不动定字提示词，也不在后面加模型步骤，碎片要在阶段一「声纹 + 识别」两路融合里解决，先把问题讲清楚再定办法。
- **判分尺修了**（Duner 09-14 同意）：行解析改为「说话人只认字母数字、后面必须紧跟冒号」，`[❓]` 挂在说话人后面算说话人不算内容。旧写法把「SPK1: 文本」拆成标签「S」+ 正文「PK1: 文本」，把「M[❓]: 文本」的「[❓]:」当正文并让这一行第一句按口径 ④ 豁免。全部归档成绩重算：单台 Qwen3 122 → 121、四路全错 50 → 49、Claude 46.0（±4）→ 46.3（±5）；DeepSeek 各行不变；拆块后 18 s 58.0 → 58.5。README 已改。证据：`tests/test_score_parse.py`。
- **说话人碎片列为远期**（Duner 09-14 定）：试过固定规则、平移、换时间戳引擎、声纹参数、声纹复核、后置 Sonnet 判定（30 s 错字 139 → 114–119，但属于用文字校准转录层的错，不接入）。项目说明远期待办已记。证据：内部仓 `实测记录/speaker_split_2026-09-14/frag/`。
- **已知问题（未修）**：DeepSeek 返回 402（余额不足）时 `fuse._call` 仍按临时错误重试，最后抛通用报错；应直接提示余额不足。

## 2026-09-12

- **Phase 3 全长三遍（基线 18 s 切块、挂 11 条术语库、同一把尺）**：
  Claude 订阅路（Opus · medium · 整篇一次）46.0 / 51.3 / 拿回 50.0 / 回退 0.3；
  DeepSeek V4.1 Flash · 8 块一批 51.0 / 58.7 / 45.0 / 0.7（$0.33/遍）；
  DeepSeek V4.1 Flash · 与 SaaS 同款两阶段 60.3 / 63.7 / 35.7 / 2.7（$0.14–0.20/遍）；
  DeepSeek 8 块一批 · 不挂库 54.7 / 64.7 / 41.3 / 1.0。
  证据：内部仓 `实测记录/p3runs_2026-09-12/`。默认工作流待 Duner 定。回流：两阶段在本地严格尺上比 8 块一批差 9 处，建议 SaaS 侧知会。
- **切块块长全长复测**：18 s 四路全错 50 / 说话人 84.4%；24 s 45 / 83.6%；30 s 42 / 80.2%。「换人下刀」切法在片段上把说话人拉到 97% 但四路全错 13→21，否决。默认块长待 Duner 定（24 或 30）。
- **候选引擎五台全长**：Fun-ASR-Nano 单台 114、作第五路全错 50→47（备选）；阿里原版 SenseVoice 123/48；SenseVoice-Nano 158/50；Cohere 268/47；Meta 1600 语 350/50。四台不动。回流：上游 `留档/未测与已否决.md` 补五条。

- **Phase 3 与 SaaS 生产线同款**（Duner 09-11 定）。融合层改为两阶段分批 + 博查联网 + Claude 订阅路；
  提示词由上游脚本生成两份（离线 / SaaS 同款）。证据：`tests/test_fuse_two_stage.py`；三遍基准待补。回流：提示词补丁改动已进上游 `sync_local_prompt.py`。
- **复读处置**（Duner 09-11 定）：双路检测（字符 + 词，阈值 10）+ 三档重试（挪窗 50 ms → 150 ms → 切半）+ 弃用该路该块。
  证据：`tests/test_repeat_loop.py`；真实模型重听 34:34 第一档救回。回流：不需要，检测器本来就搬自上游。
- **热词不做**（Duner 09-11 定）：`terms.inject_p1_hotwords` 与 SeACo 分支摘掉。回流：上游 `留档/未测与已否决.md` 加一条。

## 2026-09-11

- **判分尺以开源仓为准**（Duner 确认）。内部四份定档文档顶部加新旧口径对照。证据：内部仓 `实测记录/baseline_2026-09-11/README.md`。
- **两处缺陷修复**：占位符「/」漏进终稿；`M[❓]:` 行的统一解析。证据：`tests/test_transcript_lines.py`。
- **P1 质检闸**搬自生产线 `Phase1_ASR_QC.py`（六项），只喊不拦。回流：SaaS 生产线自己没接上这一套，建议跟。
- **基线快照** `baseline_2026-09-11`：单台 96 / 118 / 114 / 122，四路全错 50；终稿 54 / 61 / 拿回 42 / 回退 0（单跑）。
