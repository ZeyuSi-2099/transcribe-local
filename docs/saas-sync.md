# 线上 → 本地 同步台账

本地版以线上 SaaS 仓为底本（Duner 2026-09-14 定）：线上代码整块拿，只换底座。

- **登记表只有一份：[`sync/saas.yaml`](../sync/saas.yaml)。** 线上每个文件拿不拿、改不改、为什么改，都写在那里。
  本文不抄那些理由，免得两份各改各的。
- **本文写的是怎么跟：** 什么时候跑巡检、结果怎么读、跟上之后怎么挪基线，以及每次巡检的记录。
- 做法照线上仓自己跟研究仓同步的那一套（线上 `server/pipeline/sync-check.sh` + `docs/vendor-sync.md`）。

## 纪律

1. **只改本仓，不改线上仓。** 发现线上的问题，记下来交给线上自己改。例如删除转录报 500 那条，见 CLAUDE.md 远期待办。
2. **线上更新后先跑巡检**，动登记为 `same` 的文件之前也先跑。
3. **本地要改一个 `same` 文件：先在 saas.yaml 把它登记成 `modify`，写清改什么，再动手。**
   否则下次 `saas_pull` 会把本地改动原样覆盖掉；巡检也会把它报成「未登记差异」。
4. **线上的新内容一律走 `saas_pull` 拿进来。** 它先过敏感扫描，要用内部仓的私有词表。巡检只比内容、不扫描。

## 跑

```bash
python3 tools/saas_sync_check.py      # 线上仓默认在 ../Transcribe.solution，别处用 --saas 或 TRANSCRIBE_SAAS
```

退出码：0 = 没有预期外的差异；1 = 有要处理的；2 = 读不了线上仓。

**基线**是 `sync/saas.yaml` 里的 `base:`，指上次跟上线上时线上仓的那个提交。
巡检拿线上现在的内容跟基线比，才分得出一处差异是「线上改过」还是「本地改过」。
线上仓还没提交的改动也算「线上改过」。

## 结果怎么读

| 标记 | 意思 | 怎么办 |
|---|---|---|
| `[已登记]` | 登记为有意不同，和线上不一样 | 预期内，不用管 |
| ⚠️ 未登记差异 · 线上改过 | 登记为原样同步，线上改了 | 跑 `saas_pull` |
| ⚠️ 未登记差异 · 本地改过 | 登记为原样同步，本地改了 | 还原；真要改就先登记成 `modify` |
| ⚠️ 要合并 `[已登记]` | 登记为有意不同，线上自基线后又改过 | 看线上改了什么（`git -C <线上仓> diff <基线> -- <线上路径>`），人工合进本地版 |
| ⚠️ 缺文件 | 登记为要拿，本地没有 | 多半是线上新加的：跑 `saas_pull` |
| ⚠️ 未登记文件 | 登记表一条规则都没匹配上 | 在 saas.yaml 加一条，决定拿不拿 |
| ⚠️ 线上删了 | 基线时线上还有，现在没了，本地还留着 | 本地一并删掉 |
| 提示 · 与线上一致 | 登记为有意不同，内容却和线上一样 | 不影响同步；确定以后也不改的，可以改回 `same` |

## 跟上线上的一轮

1. `python3 tools/saas_sync_check.py`：看线上自基线改过几个文件，各落在哪一类。
2. `TRANSCRIBE_SCAN_WORDS=<内部仓私有词表> python3 tools/saas_scan.py`：线上新加的文件先登记，新加的内容先过扫描。
   要做到未登记 0、待看 0。
3. `TRANSCRIBE_SCAN_WORDS=… python3 tools/saas_pull.py --part backend`，再跑一遍 `--part frontend`：
   `same` 覆盖，`modify` 本地已有就不动。
4. 「要合并」的逐个人工合。
5. `python3 tools/saas_pp_prompts.py --check`：后处理提示词在线上 vendor 目录里，登记为不拿，巡检看不到它，所以单独查。
   报「改过了」就去掉 `--check` 重新生成。
6. 跑全部测试（命令见 CLAUDE.md），界面有改动的在浏览器里走一遍。
7. 巡检只剩 `[已登记]` 之后，跑 `python3 tools/saas_sync_check.py --mark-synced`，把基线挪到线上当前提交，
   和这一轮的改动一起提交。
   - 还有未处理的项时，这一步会拒绝。
   - 线上仓有没提交的改动时，这一步也会拒绝。

## 线上搬来的测试：本机用不上的怎么标

规则（Duner 2026-09-15 确认）：

- **本地用得上的，改到通过。** 改过的测试文件登记为 `modify`。
- **本地根本没有那个功能的用例，登记为「不适用」。** 登记在 `backend/conftest.py` 的 `NOT_APPLICABLE`，写明理由；
  跑的时候跳过，结果末尾会汇总。
- **只看本地有没有这个功能，不看测试过不过、好不好改。**

两个细节：

- 判断的单位是一条条用例。同一个文件里，常常一部分照跑、一部分不适用。
- 登记了却对不上的（线上把用例改名或删了），那次测试直接判失败，免得登记表悄悄过期。

同步时线上的测试文件有改动，先看新改的用例属于哪种：本地有那个功能就让它跑通，没有才登记。

## 巡检记录

### 2026-09-15 · 运行面板本地化

- **修掉的问题：**
  - 「工作流」页签的接口一调就报错（500），它要导入云端编排；
  - 「近况」与术语库助手耗时统计用的是 Postgres 聚合写法，在 SQLite 上查询出错、被吞掉，页面恒空；
  - 「运行」页签展开一单时各引擎一栏只认线上代号，本机四路一个都不显示。
- **改成本机的：**
  - 工作流节点照本机流水线画，参数读实际生效的配置；
  - 「资源」换成「这台电脑」（模型、磁盘、内存、模型后端、数据去哪）；
  - 去掉 Claude 订阅健康度与配置、证书到期、云机器容量、登录验证码用量。
- 运行面板那两份线上测试改到通过（modify），登录发码量、证书到期、语种编排、指纹口径登记为不适用。
- 结果：已登记 71 · 一致 142 · 与线上一致 5 · 待定 2（只剩前端 e2e 走查脚本与说明）· **要处理 0**。
- 后端整目录 686 过、127 跳过、**0 失败**；前端 566 过。

### 2026-09-15 · 待定测试清理

- 按上面的规则处理了 36 个待定文件：
  - 11 个本来就能过；
  - 9 个改成本机版；
  - 用例级的不适用 117 组，整份不适用 6 个文件；
  - 线上 docker 跑法的 2 份说明不拿。
- 结果：已登记 65 · 一致 144 · 与线上一致 7 · 待定 4 · **要处理 0**。
- 后端整目录：666 过、121 跳过（都是不适用），15 条报错全在还待定的两份运行面板测试里。
- **还待定的 4 个：**
  - **运行面板两份测试。** 它们跑不通，是因为运行面板本身在本机坏了：
    - 「工作流」页签的接口一调就报错（500），它要导入云端编排模块；
    - 「近况」的引擎、定字、后处理统计用的是 Postgres 的 JSON 写法，在 SQLite 上查询出错，被静默吞掉，页面上一直是空的。
  - **界面的端到端走查脚本和它的说明。** 要按本机流程改，并装 Playwright。
- 顺带改了一处后台代码：转录入口按线上写法做成模块级的 `worker.transcribe`，线上检查可以按这个名字打桩，行为不变。

### 2026-09-15 · 建台账

- 基线 `0c9d0da81110`（线上 2026-09-12 的提交；第 2–5 步都是从这个提交搬的，之后线上没有新提交）。
- 结果：已登记 55 · 一致 122 · 待定 38 · 与线上一致 7 · **要处理 0**。
- **待定 38 个**：
  - 32 个是第 2 步搬进来的连库、云端配置类测试，还没逐个定成 `same`、`modify` 或删除。
  - 另 6 个原本登记为 `same`，这次发现它们在本机跑不通，改为待定：
    - `test_pp_runner`、`test_pp_lang`、`test_ds_pricing`、`test_postprocess`、`test_postprocess_api`、`test_workflow_view`；
    - 内容与线上一致，但测的是本地没搬的结账、vendor 目录与证书到期模块。
- **后端整目录跑测试**（`cd backend && python3 -m pytest -m "not integration"`）：530 过、98 失败、36 报错。
  - 失败和报错都出在上面这些待定测试里。
  - 与第 6 步开始前逐条对过，是同一批，没有新增。
- **与线上一致 7 个**：
  - 运行面板那 5 个（`alerts` `health_view` `p3_config` `p3_probe` `workflow_view`），等「资源」标签改成看本机时再改。
  - `claude_gate`、`postprocess`：登记时预计要改，实际没用上。先保持 `modify`，线上改了会报「要合并」，不会被悄悄覆盖。
- **验收**：在线上仓的一个临时克隆里各改一行，线上仓本身没动。
  - 改了登记为 `same` 的 `server/app/export.py`，报「未登记差异 · 线上改过」。
  - 改了登记为 `modify` 的 `src/screens/main/Result.tsx`，报「要合并 [已登记]」。
